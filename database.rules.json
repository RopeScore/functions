{
  "rules": {
    ".read": false,
    ".write": false,
    "graphql-firebase-subscriptions": {
      "$topic": {
        ".indexOn": "timestamp",
        "timestamp": {
          ".validate": "newData.isNumber()"
        }
      }
    }
  }
}
